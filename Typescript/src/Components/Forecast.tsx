import { forecastType } from "../Types"

type Props = {
    data: forecastType
}

const Forecast = ({ data }: Props): JSX.Element => {
  return (
    <div>
      
    </div>
  )
}

export default Forecast
